# Day 04｜渠道接入与白名单机制

## 今日主题
把“谁都能发消息给系统”升级为“只有被授权的人能触发可执行动作”。

OpenClaw 的渠道接入不是单纯把 Telegram、Web 或 API 连上就结束，而是要在入口第一层就完成**身份识别与白名单校验**。这决定了系统是“可用”还是“可控”。

---

## 技术原理（技术视角）

### 1) 渠道接入的本质：把外部输入映射为可治理消息
外部渠道（聊天工具、Web 表单、API 回调）本质都只是“输入载体”。真正进入 OpenClaw 后，会被统一为消息对象，随后交由会话与工具链路处理。

关键点在于：
- 渠道字段要标准化（发送者、来源、会话ID、原始文本）；
- 标准化后才能做一致的权限判断和审计记录；
- 入口越统一，后续治理成本越低。

### 2) 白名单机制的作用顺序：先校验，再执行
白名单是**入口准入控制**，不是事后追责工具。正确顺序应是：
1. 接收消息；
2. 识别来源身份（用户ID、群组ID、角色）；
3. 匹配白名单规则；
4. 通过才进入执行链路。

如果顺序错成“先执行再判断”，就会出现高风险动作被误触发的问题（例如外发通知、批量写入、调用高权限命令）。

### 3) 关键组件
- **消息模型（Message）**：承接来自不同渠道的原始输入；
c
2. **外部协作渠道接入**  
   接入合作方渠道时，仅开放只读查询能力；写入类动作保留在内部白名单渠道，避免跨组织误操作。

---

## 官方资料（本地镜像优先）

### 本地文档（知识库镜像路径）
- [[OpenClaw-30天学习手册/官方文档-本地镜像(节选)/concepts__messages.md|concepts__messages.md]]  
  看点：消息如何在系统内被统一建模并进入执行流程。
- [[OpenClaw-30天学习手册/官方文档-本地镜像(节选)/concepts__session.md|concepts__session.md]]  
  看点：会话如何承接身份、上下文和执行边界。
- [[OpenClaw-30天学习手册/官方文档-本地镜像(节选)/concepts__context.md|concepts__context.md]]  
  看点：上下文变量如何影响权限判断和工具调用。
- [[OpenClaw-30天学习手册/官方文档-本地镜像(节选)/start__setup.md|start__setup.md]]  
  看点：接入与运行时配置中的基础安全项。

### 在线文档
- https://docs.openclaw.ai/concepts/messages
- https://docs.openclaw.ai/concepts/session
- https://docs.openclaw.ai/concepts/context
- https://docs.openclaw.ai/start/setup
- https://github.com/openclaw/openclaw

---

## 图示
![[OpenClaw-30天学习手册/assets/day04-channel-whitelist-flow.svg]]

图示阅读要点：
- 白名单校验位于执行前，是“入口闸门”；
- 通过与拒绝都要进入日志，形成完整审计链路；
- 拒绝路径不是失败，而是治理能力的一部分。

---

## 管理者关注点
- **成本**：一次性建立统一准入规则，降低后续跨团队沟通与返工成本；
- **效率**：高频、低风险请求自动放行，减少人肉审批；
- **风险**：把高风险动作的触发面收窄到受控渠道和受控身份，避免“误触发即事故”。

---

## 本日自检
1. 我是否能说清“渠道接入”和“权限治理”是两层能力，而不是一件事？
2. 我是否能画出“先鉴权后执行”的顺序，并解释为何不能反过来？
3. 我是否为当前常用渠道定义了最小白名单策略（人、群、动作三维）？

---

## 一句话总结
渠道接入解决的是“能进来”，白名单机制解决的是“谁能动、动什么”，两者结合才是可规模化的自动化入口。